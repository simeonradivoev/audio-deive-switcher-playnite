<UserControl x:Class="AudioDeviceSwitcher.AudioDeviceSwitcherSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    
    <StackPanel Margin="20">
        <TextBlock Text="{DynamicResource ADSPluginFullscreenSwitchingTitle}" Style="{StaticResource BaseTextBlockStyle}" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <CheckBox Content="{DynamicResource ADSPluginEnableFullscreenSwitching}"
                  IsChecked="{Binding Settings.EnableFullscreenSwitch}"
                  Margin="0,0,0,20"/>

        <TextBlock Text="{DynamicResource ADSPluginFullscreenDeviceLabel}" Margin="0,0,0,5"/>
        
        <ComboBox ItemsSource="{Binding AvailableDevices}"
                  SelectedValue="{Binding Settings.FullscreenDeviceId}"
                  DisplayMemberPath="Name"
                  SelectedValuePath="Id"
                  IsEnabled="{Binding Settings.EnableFullscreenSwitch}"/>
        
        <TextBlock Text="{DynamicResource ADSPluginFullscreenExitTip}"
                   Margin="0,20,0,0" FontStyle="Italic"
                   TextWrapping="Wrap" Opacity="0.7"/>
    </StackPanel>
</UserControl>